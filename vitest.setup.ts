import '@testing-library/jest-dom/vitest';

// Silence Next.js router warnings in tests
// and provide minimal mocks where useful.
// Individual tests can override with vi.mock('next/navigation', ...)

